import UIKit

struct Company {
    let name: String
    let address: String
    let zip: String
    
    init(name:String,address:String,zip:String) {
        self.name = name
        self.address = address
        self.zip = zip
    }
    
}
