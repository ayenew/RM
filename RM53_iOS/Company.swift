import UIKit

struct Company {
    let name: String
    let address: String
    let city: String
    
    init(name:String,address:String,city:String) {
        self.name = name
        self.address = address
        self.city = city
    }
    
}
